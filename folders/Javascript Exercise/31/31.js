//31

let username=['admin', 'ahmad', 'ali', 'arham', 'usman'];

username.pop();
username.pop();
username.pop();
username.pop();
username.pop();

for (let i=0; i< username.length;i++){
if (username.length == 0){
    console.log('We need to find some users!');
}
else{
    console.log('array is not empty');
}
}
